import { device } from "@/styles/media";
import styled from "styled-components";

export const BlogInputWrapper = styled.input`
  border: 0.5px solid black;
  padding: 10px 20px;
  border-radius: 3px;
  font-weight: 300;
`;

export const BlogTextAreaWrapper = styled.textarea`
  border: 0.5px solid black;
  padding: 10px 20px;
  border-radius: 3px;
  font-weight: 300;
`;
