import { styled } from "styled-components";

export const NewDetailBarWrapper = styled.div`
  display: flex;
  justify-content: space-between;

  p {
    font-size: 12px !important;
    font-family: var(--blog-paragraph-font) !important;
  }
`;
